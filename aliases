alias migrate="rake db:migrate db:test:prepare"
alias gadd="git add -u && git add . && git status"
alias g="git"
alias gci="git commit -m"
alias gst="git status -s"
alias gaci="gadd && gci"

alias bi="bundle install"
alias bo="bundle open"

os_type=`uname`

# Darwin doesn't support --color attribute
if [ "$os_type" != Darwin ]; then
  ls_options="--color=auto"
fi

export CLICOLOR=true
alias l="ls -CFlhG $ls_options"
alias ll="ls -lG $ls_options"
alias la="ls -alhG $ls_options"

alias duply_backup="cd && tar -cz .duply | gpg -c --output ~/Dropbox/backups/duply_config.tgz.gpg"

alias edit_config="subl ~/.config_files"
alias edit_subl_packages="subl ~/Library/Application\ Support/Sublime\ Text\ 2/Packages/"


# work-stuff
alias cdm='cd $HOME/code/xws && eval `bin/xing_env` && export GEM_HOME=$HOME/.gem/ruby/1.8'
alias cdr='cd $HOME/code/rails-app && eval `bin/xing_env` && export GEM_HOME=$HOME/.gem/ruby/1.8'
alias cdp='cd $HOME/code/people_directory && PATH=/Users/mark.schmidt/Downloads/redis-2.0.2:$PATH'

alias rwst="rake web_service:test"
alias rxt="rake xws:test"
